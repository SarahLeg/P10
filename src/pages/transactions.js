//ici le composant account
// + les données précises de celui-ci dans des composants collapse ? à l'intérieur du collapse de quoi prendre des notes (???)

import React from 'react';
import Account from '../components/account';

function Transactions() {
    return (
    <div>
        <Account title="Argent Bank Checking (x3448)" amount="48,098.43" description="Available Balance"/>

    </div>
    );
}

export default Transactions;